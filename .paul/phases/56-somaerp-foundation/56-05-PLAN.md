---
phase: 56-somaerp-foundation
plan: 05
type: execute
wave: 1
depends_on: ["56-03", "56-04"]
files_modified:
  - apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/02_in_progress/20260424_004_create-raw-materials-suppliers.sql
  - apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_raw_material_categories.yaml
  - apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_units_of_measure.yaml
  - apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_supplier_source_types.yaml
  - apps/somaerp/backend/02_features/30_raw_materials/__init__.py
  - apps/somaerp/backend/02_features/30_raw_materials/schemas.py
  - apps/somaerp/backend/02_features/30_raw_materials/repository.py
  - apps/somaerp/backend/02_features/30_raw_materials/service.py
  - apps/somaerp/backend/02_features/30_raw_materials/routes.py
  - apps/somaerp/backend/02_features/35_suppliers/__init__.py
  - apps/somaerp/backend/02_features/35_suppliers/schemas.py
  - apps/somaerp/backend/02_features/35_suppliers/repository.py
  - apps/somaerp/backend/02_features/35_suppliers/service.py
  - apps/somaerp/backend/02_features/35_suppliers/routes.py
  - apps/somaerp/backend/main.py
  - apps/somaerp/03_docs/features/11_somaerp/feature.manifest.yaml
  - apps/somaerp/frontend/src/app/page.tsx
  - apps/somaerp/frontend/src/app/supply/page.tsx
  - apps/somaerp/frontend/src/app/supply/raw-materials/page.tsx
  - apps/somaerp/frontend/src/app/supply/raw-materials/new/page.tsx
  - apps/somaerp/frontend/src/app/supply/suppliers/page.tsx
  - apps/somaerp/frontend/src/app/supply/suppliers/new/page.tsx
  - apps/somaerp/frontend/src/types/api.ts
  - apps/somaerp/frontend/src/lib/api.ts
autonomous: true
---

<objective>
## Goal

Ship RAW MATERIALS + SUPPLIERS vertical end-to-end: 3 dim tables (categories, units_of_measure, supplier_source_types) + 3 fct tables (raw_materials, raw_material_variants, suppliers) + 1 mutable link (raw_material_suppliers with is_primary toggle) + 3 views. Seed ~20 Soma Delights raw materials + 4 suppliers (Bowenpally, Rythu Bazaar Kukatpally, Erragadda, BigBasket) + primary/backup mappings per operations-model.md.

## Purpose

Unblocks 56-06 (kitchen_capacity needs dim_units_of_measure), 56-07 (recipes need raw_material_ids + units), 56-08 (QC checkpoints reference raw_materials), 56-09 (procurement runs + inventory movements reference raw_materials + suppliers). Plus: `dim_units_of_measure` is cross-cutting throughout the rest of phase 56.

## Output

- Migration + 3 seed YAMLs (categories, units, source_types)
- 2 sub-features: `30_raw_materials` (materials + variants + categories + units reads) + `35_suppliers` (suppliers + source_types read + lnk attach/detach/set_primary)
- 15+ endpoints under /v1/somaerp/supply/*
- 5 frontend pages under /supply/*
- Soma Delights seed via API: 17 raw materials + 4 suppliers + ~10 primary-supplier links
- MCP walk of /supply/raw-materials + /supply/suppliers with real data
</objective>

<context>
@.paul/phases/56-somaerp-foundation/56-CONTEXT.md
@.paul/phases/56-somaerp-foundation/56-03-SUMMARY.md
@.paul/phases/56-somaerp-foundation/56-04-SUMMARY.md
@apps/somaerp/03_docs/01_data_model/05_raw_materials.md
@apps/somaerp/03_docs/02_api_design/05_raw_materials.md
@apps/somaerp/03_docs/05_tenants/01_somadelights_tenant_config.md
@99_business_refs/somadelights/05-operations/operations-model.md
@99_business_refs/somadelights/05-operations/supplier-vendor-directory.md

## Proven templates (mirror 1:1)
@apps/somaerp/backend/02_features/20_product_lines/{schemas,repository,service,routes}.py
@apps/somaerp/backend/02_features/25_products/{schemas,repository,service,routes}.py
@apps/somaerp/frontend/src/app/catalog/{page.tsx,product-lines/page.tsx,product-lines/new/page.tsx}
@apps/somaerp/frontend/src/lib/api.ts
@apps/somaerp/frontend/src/types/api.ts

## tennetctl conventions
@.claude/rules/common/database.md
@.claude/rules/python.md
</context>

<acceptance_criteria>

## AC-1: Migration creates 6 tables + 1 link + 3 views; 3 seeds applied
```gherkin
Given 56-04 migration applied
When migration 004 runs
Then schema "11_somaerp" has: dim_raw_material_categories, dim_units_of_measure (with dimension + base_unit_id self-FK + to_base_factor), dim_supplier_source_types, fct_raw_materials, fct_raw_material_variants, fct_suppliers, lnk_raw_material_suppliers
And views v_raw_materials, v_suppliers, v_raw_material_supplier_matrix exist
And seeds: >= 7 categories, >= 7 units (kg/g/l/ml/count/bunch/bottle with dimension + base_factor), >= 6 supplier source types
And lnk_raw_material_suppliers IS mutable (has updated_at, updated_by) — spec explicitly deviates from immutable-lnk rule; document in ADR-or-SUMMARY
And partial unique "exactly one primary supplier per material" enforced
And partial unique "exactly one default variant per material" enforced
```

## AC-2: 15+ endpoints expose envelope + audit; primary supplier toggle works atomically
```gherkin
Given sub-features mounted
When POST /v1/somaerp/supply/raw-materials with tag_codes
Then row created in fct_raw_materials + audit .materials.created
When POST /v1/somaerp/supply/raw-materials/{id}/suppliers with is_primary=true
Then lnk row created + audit .material_supplier.linked (and if another lnk was primary, it's cleared atomically)
When PATCH /v1/somaerp/supply/raw-materials/{id}/suppliers/{supplier_id} with is_primary=true
Then prior primary's is_primary=false IN SAME TX + audit .primary_changed
When DELETE /v1/somaerp/supply/raw-materials/{id}/suppliers/{supplier_id}
Then lnk row deleted + audit .unlinked
```

## AC-3: Frontend /supply/* renders with live data
```gherkin
Given services running authed
And Soma Delights materials + suppliers seeded via API
When visiting /supply/raw-materials
Then table shows 17 materials with category + default_unit + status
When visiting /supply/suppliers
Then table shows 4 suppliers with source_type + location + status
And tsc + npm build green
```

## AC-4: MCP walk with authed session verifies both /supply pages with live data

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Backend — migration + 3 seeds + 2 sub-features + main.py mount + manifest</name>
  <files>
    apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/02_in_progress/20260424_004_create-raw-materials-suppliers.sql,
    apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_raw_material_categories.yaml,
    apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_units_of_measure.yaml,
    apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_supplier_source_types.yaml,
    apps/somaerp/backend/02_features/30_raw_materials/__init__.py,
    apps/somaerp/backend/02_features/30_raw_materials/schemas.py,
    apps/somaerp/backend/02_features/30_raw_materials/repository.py,
    apps/somaerp/backend/02_features/30_raw_materials/service.py,
    apps/somaerp/backend/02_features/30_raw_materials/routes.py,
    apps/somaerp/backend/02_features/35_suppliers/__init__.py,
    apps/somaerp/backend/02_features/35_suppliers/schemas.py,
    apps/somaerp/backend/02_features/35_suppliers/repository.py,
    apps/somaerp/backend/02_features/35_suppliers/service.py,
    apps/somaerp/backend/02_features/35_suppliers/routes.py,
    apps/somaerp/backend/main.py,
    apps/somaerp/03_docs/features/11_somaerp/feature.manifest.yaml
  </files>
  <action>
    READ spec (apps/somaerp/03_docs/01_data_model/05_raw_materials.md + 02_api_design/05_raw_materials.md) + 56-04 template (apps/somaerp/backend/02_features/{20_product_lines,25_products}/) before writing.

    **Migration `20260424_004_create-raw-materials-suppliers.sql`**:
    - UP: CREATE TABLE in "11_somaerp":
      - dim_raw_material_categories (SMALLINT PK, code UNIQUE, name, deprecated_at)
      - dim_units_of_measure (SMALLINT PK, code UNIQUE, name, dimension TEXT NOT NULL CHECK IN ('mass','volume','count'), base_unit_id SMALLINT REFERENCES dim_units_of_measure(id), to_base_factor NUMERIC(20,8))
      - dim_supplier_source_types (SMALLINT PK, code UNIQUE, name, deprecated_at)
      - fct_raw_materials (UUID PK + tenant_id + category_id FK + name + slug + default_unit_id FK + default_shelf_life_hours + requires_lot_tracking BOOL DEFAULT TRUE + target_unit_cost NUMERIC(14,4) + currency_code CHAR(3) + status CHECK active/paused/discontinued + properties JSONB + audit/ts/soft-delete)
      - fct_raw_material_variants (UUID PK + tenant_id + raw_material_id FK + name + slug + target_unit_cost + currency_code + is_default BOOL + status CHECK active/paused + properties + audit/ts/soft-delete)
      - fct_suppliers (UUID PK + tenant_id + name + slug + source_type_id FK + location_id UUID REFERENCES "11_somaerp".fct_locations(id) nullable + contact_jsonb JSONB + payment_terms TEXT + default_currency_code CHAR(3) + quality_rating SMALLINT CHECK 1-5 + status CHECK active/paused/blacklisted + properties + audit/ts/soft-delete)
      - lnk_raw_material_suppliers (UUID PK + tenant_id + raw_material_id FK + supplier_id FK + is_primary BOOL + last_known_unit_cost + currency_code + notes + created_at + updated_at + created_by + updated_by — NOTE: MUTABLE link per spec deviation; keeps updated_at unlike standard lnk_* immutable rule)
    - 3 views: v_raw_materials (join category + default_unit), v_suppliers (join source_type + location nullable), v_raw_material_supplier_matrix (join material + supplier for procurement planner)
    - Indexes per spec § Indexes
    - Partial unique indexes: (tenant_id, slug) WHERE deleted_at IS NULL on fct_raw_materials + fct_suppliers; (tenant_id, raw_material_id, slug) WHERE deleted_at IS NULL on variants; (tenant_id, raw_material_id) WHERE is_default AND deleted_at IS NULL on variants (one default); (tenant_id, raw_material_id, supplier_id) UNIQUE on lnk; (tenant_id, raw_material_id) WHERE is_primary on lnk (exactly-one-primary).
    - Migration file uses `-- UP` as first content line WITHOUT a separator after (fix the 56-04 parser gotcha — prefer `-- UP\n` marker without === suffix, OR runner fix handles it either way)
    - DOWN drops in reverse dep order (views first, then lnk, variants, fcts, dims)
    - TIMESTAMP + CURRENT_TIMESTAMP per project rule

    **Seed YAMLs** (format matching conftest.py _apply_seed: `{schema: "11_somaerp", table: "dim_...", rows: [...]}` OR `{table: "11_somaerp.dim_...", rows: [...]}` — match whichever format existing 56-03/56-04 seeds use):
    - `11somaerp_dim_raw_material_categories.yaml`: rows (1, leafy_green, Leafy Green), (2, root, Root), (3, fruit, Fruit), (4, herb, Herb), (5, spice, Spice), (6, packaging, Packaging), (7, label, Label), (8, consumable, Consumable)
    - `11somaerp_dim_units_of_measure.yaml`: MUST insert base units first so base_unit_id FK resolves.
      - (1, kg, Kilogram, mass, null, 1)  — base unit for mass
      - (2, g, Gram, mass, 1, 0.001)  — references kg
      - (3, l, Liter, volume, null, 1)  — base unit for volume
      - (4, ml, Milliliter, volume, 3, 0.001)  — references l
      - (5, count, Count, count, null, 1)
      - (6, bunch, Bunch, count, 5, 1)
      - (7, bottle, Bottle, count, 5, 1)
    - `11somaerp_dim_supplier_source_types.yaml`: (1, wholesale_market), (2, rythu_bazaar), (3, farm_direct), (4, marketplace), (5, brand_distributor), (6, online)

    **Sub-feature `30_raw_materials/`** (5-file pattern, mirror 25_products structure):
    - schemas.py: RawMaterialCategoryOut, UnitOfMeasureOut, RawMaterialCreate/Update/Out (with category_name, default_unit_code), RawMaterialVariantCreate/Update/Out (with is_default, raw_material_name)
    - repository.py: list_categories, list_units, list_materials (filters: category_id/status/q/requires_lot_tracking), get_material, create_material, update_material, soft_delete_material, list_variants(material_id), get_variant, create_variant (atomic is_default replacement), update_variant (same atomic), soft_delete_variant. Cross-tenant filter at repo. uuid7. Variant partial-unique-default enforced via pre-clear in same tx.
    - service.py: audit emission — .materials.created/.updated/.status_changed/.deleted; .variants.created/.updated/.deleted. (pool, conn, ctx) signature. TODO(56-07) for "cannot delete material with active recipes" guard since fct_recipes doesn't exist yet.
    - routes.py: APIRouter prefix="/v1/somaerp/supply" tags=["supply","raw_materials"]. Endpoints: GET /raw-material-categories (read), GET /units-of-measure (read), GET /raw-materials (filter), POST /raw-materials, GET /raw-materials/{id}, PATCH /raw-materials/{id}, DELETE /raw-materials/{id}, GET /raw-materials/{id}/variants, POST /raw-materials/{id}/variants, GET/PATCH/DELETE /raw-materials/{id}/variants/{variant_id}. Bulk POST supported for /raw-materials.

    **Sub-feature `35_suppliers/`** (5-file pattern):
    - schemas.py: SupplierSourceTypeOut, SupplierCreate/Update/Out (with source_type_code, source_type_name, location_name when location_id set), SupplierMaterialLinkCreate/Update/Out (is_primary, last_known_unit_cost, currency_code, notes, plus material_name + supplier_name for list views).
    - repository.py: list_source_types, list_suppliers (filters: source_type_id/status/location_id/q), get_supplier, create_supplier (validate location_id is None OR exists AND same tenant — 422 INVALID_LOCATION otherwise), update_supplier, soft_delete_supplier. For link table: list_supplier_links(material_id), list_material_suppliers(supplier_id), get_link, create_link (atomic: if is_primary=true, UPDATE current primary SET is_primary=false WHERE raw_material_id=$1 AND is_primary=true), update_link (same atomic if toggling primary to true), delete_link (HARD delete on lnk since there's no deleted_at). Validate both raw_material_id + supplier_id same tenant at repo.
    - service.py: emits .suppliers.created/.updated/.status_changed/.deleted; .material_supplier.linked/.unlinked/.primary_changed. Primary change emits .primary_changed (NOT .updated). Cross-layer validate: location_id must exist if provided.
    - routes.py: APIRouter prefix="/v1/somaerp/supply" tags=["supply","suppliers"]. Endpoints: GET /supplier-source-types (read), GET /suppliers (filter), POST /suppliers, GET /suppliers/{id}, PATCH /suppliers/{id}, DELETE /suppliers/{id}, GET /suppliers/{id}/materials (list what they supply), GET /raw-materials/{id}/suppliers (list who supplies this; NOTE: this shares the /raw-materials URL root with 30_raw_materials but is in 35_suppliers's router — FastAPI allows multiple routers to share prefix), POST /raw-materials/{id}/suppliers (link), PATCH /raw-materials/{id}/suppliers/{supplier_id} (update is_primary/cost), DELETE /raw-materials/{id}/suppliers/{supplier_id} (unlink).

    **main.py**: add 2 more import_module + include_router calls for 30_raw_materials + 35_suppliers.

    **feature.manifest.yaml**: append 11_somaerp.30_raw_materials + 11_somaerp.35_suppliers.

    Boundaries: no changes outside the listed files. No uuid4. TIMESTAMP. properties JSONB on every fct_*. lnk_raw_material_suppliers HAS updated_at (per spec deviation) — flag this in SUMMARY as ADR candidate. No external SaaS deps.
  </action>
  <verify>cd /Users/sri/Documents/tennetctl && python3 -c "
import ast
for sf in ['30_raw_materials','35_suppliers']:
    for m in ['schemas','repository','service','routes']:
        ast.parse(open(f'apps/somaerp/backend/02_features/{sf}/{m}.py').read())
ast.parse(open('apps/somaerp/backend/main.py').read())
import yaml
for k in ['raw_material_categories','units_of_measure','supplier_source_types']:
    yaml.safe_load(open(f'apps/somaerp/03_docs/features/11_somaerp/05_sub_features/30_raw_materials/09_sql_migrations/seeds/11somaerp_dim_{k}.yaml'))
m = yaml.safe_load(open('apps/somaerp/03_docs/features/11_somaerp/feature.manifest.yaml'))
keys = {s['key'] for s in m['sub_features']}
assert '11_somaerp.30_raw_materials' in keys and '11_somaerp.35_suppliers' in keys
print('OK')
"</verify>
  <done>AC-1 + AC-2 partial; files parse; manifest + main.py updated; 3 seeds valid YAML</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — /supply landing + raw-materials + suppliers pages + types/lib</name>
  <files>
    apps/somaerp/frontend/src/app/page.tsx,
    apps/somaerp/frontend/src/app/supply/page.tsx,
    apps/somaerp/frontend/src/app/supply/raw-materials/page.tsx,
    apps/somaerp/frontend/src/app/supply/raw-materials/new/page.tsx,
    apps/somaerp/frontend/src/app/supply/suppliers/page.tsx,
    apps/somaerp/frontend/src/app/supply/suppliers/new/page.tsx,
    apps/somaerp/frontend/src/types/api.ts,
    apps/somaerp/frontend/src/lib/api.ts
  </files>
  <action>
    Mirror the /catalog pattern from 56-04 exactly.

    - UPDATE page.tsx: add "Supply" module card next to Geography + Catalog. Preserve health widget + existing nav.
    - CREATE /supply/page.tsx: 2 nav cards (Raw Materials, Suppliers).
    - CREATE /supply/raw-materials/page.tsx: filter on category + status; table with Name, Category, Default Unit, Lot-Tracked (boolean pill), Target Cost + Currency, Status.
    - CREATE /supply/raw-materials/new/page.tsx: form — Category (select), Name, Slug (auto), Default Unit (select), Default Shelf Life hours, Requires Lot Tracking (checkbox, default true), Target Unit Cost (number), Currency (default INR), Status.
    - CREATE /supply/suppliers/page.tsx: filter on source_type + status + location; table with Name, Source Type (badge), Location (nullable), Payment Terms, Quality Rating (1-5 stars), Status.
    - CREATE /supply/suppliers/new/page.tsx: form — Source Type (select), Name, Slug (auto), Location (combobox from listLocations, optional with "— none —"), Contact (textarea JSON), Payment Terms (select: cash_on_delivery / net_7 / prepaid / net_30), Default Currency (default INR), Quality Rating (1-5 select), Status (active default).

    APPEND to types/api.ts: RawMaterialCategory, UnitOfMeasure, SupplierSourceType, RawMaterial, RawMaterialVariant, Supplier, SupplierMaterialLink (+ status literals).

    APPEND to lib/api.ts (keep Bearer-auth apiFetch + all prior wrappers): listRawMaterialCategories, listUnitsOfMeasure, listRawMaterials, createRawMaterial, listSupplierSourceTypes, listSuppliers, createSupplier.

    No `any`. No mock data. npm run build + tsc must be clean.
  </action>
  <verify>cd /Users/sri/Documents/tennetctl/apps/somaerp/frontend && npx tsc --noEmit 2>&1 | tail -3 && npm run build 2>&1 | tail -5</verify>
  <done>AC-3 satisfied</done>
</task>

<task type="auto">
  <name>Task 3: Apply migration + seed dims + restart backend + MCP walk (NO pytest — deferred to 56-05b like 56-04b)</name>
  <files></files>
  <action>
    No new files. Live operator actions.

    1. Apply migration 004 to live dev DB (same pattern as 56-04 Task 3): read file, split on `-- UP\n` (skip marker line), execute UP, then INSERT seeds from 3 YAML files.

    2. Kill + restart somaerp backend so new routes mount.

    3. Refresh admin token via tennetctl /v1/auth/signin (tokens invalidate on somaerp restart because SessionProxyMiddleware re-validates).

    4. Create suppliers + raw materials + links via API:
       - 4 suppliers: Bowenpally Wholesale Market (wholesale_market, location=Hyderabad, cash_on_delivery), Rythu Bazaar Kukatpally (rythu_bazaar, location=Hyderabad), Erragadda Market (wholesale_market, location=Hyderabad), BigBasket (marketplace, no location, net_7)
       - ~17 raw materials per operations-model.md procurement table: spinach (leafy_green, kg), mint (herb, bunch), coriander (herb, bunch), celery (leafy_green, kg), carrot (root, kg), beetroot (root, kg), ginger (root, kg), fresh turmeric (root, kg), green apple (fruit, kg), lemon (fruit, count), cucumber (fruit, kg), orange (fruit, kg), pineapple (fruit, count), black pepper (spice, g), 300ml PET bottle (packaging, bottle), tamper-evident cap (packaging, count), branded label (label, count). Default currency INR.
       - Link primary suppliers: spinach ↔ Bowenpally (primary) + Rythu Bazaar (backup); similar for 5-6 key materials. BigBasket as universal backup.

    5. Update MCP localStorage token, navigate to /supply/raw-materials + /supply/suppliers, take screenshots showing real data.

    6. Verify frontend dev server isn't cache-stale (if it is, rm -rf .next + restart).

    Report what each step produced + any errors encountered.
  </action>
  <verify>curl -s "http://localhost:51736/v1/somaerp/supply/raw-material-categories" | head -c 200; echo; curl -s "http://localhost:51736/v1/somaerp/supply/units-of-measure" | head -c 200</verify>
  <done>AC-4 via MCP screenshots; migration live; seed via API</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Anything outside listed files
- Existing sub-features (00_health, 10_locations, 15_kitchens, 17_service_zones, 20_product_lines, 25_products)
- Existing migrations + seeds from 56-03/56-04

## SCOPE LIMITS
- NO kitchen_capacity work (that's 56-06; now unblocked by this plan's dim_units_of_measure)
- NO recipe/procurement/production tables
- NO pytest (deferred to 56-05b like 56-04b)
- NO RBAC enforcement
- NO map/picker UI
- lnk_raw_material_suppliers IS mutable (has updated_at) — documented spec deviation

</boundaries>

<verification>
- Migration + 3 seeds applied
- 15+ endpoints mounted under /v1/somaerp/supply
- 5 frontend pages + types + lib appended
- npm build + tsc green
- MCP walk screenshots of both /supply pages with real data
</verification>

<output>
Create .paul/phases/56-somaerp-foundation/56-05-SUMMARY.md with: files, ACs, deviations (lnk_* mutability exception; pytest deferred), MCP evidence, loop position → ready for UNIFY. Then 56-05 UNIFY can proceed, and 56-06 kitchen_capacity unblocks next.
</output>
