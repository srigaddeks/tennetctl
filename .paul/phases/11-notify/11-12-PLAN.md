# Plan 11-12: Robot E2E Full Flow + Commit

**Phase:** 11 (Notify)
**Depends on:** 11-11
**Autonomous:** true

## Objective

Add Robot Framework E2E tests covering the full Notify UI surface, then commit the entire Phase 11 + Phase 12 auth baseline as one commit.

## Acceptance Criteria

- [ ] `tests/e2e/notify/12_notify_pages.robot` — smoke tests: campaigns, deliveries, send, preferences pages all render
- [ ] All existing template designer tests still pass
- [ ] STATE.md and ROADMAP.md updated to reflect Phase 11 complete
- [ ] Git commit created with all Phase 11 changes

## Tasks

### Task 1: Robot E2E — notify pages smoke tests

**Action:** Create `tests/e2e/notify/12_notify_pages.robot` with test cases for:
- Campaigns page loads (heading + new button + nav link)
- Deliveries page loads (heading + filter controls + nav link)
- Send API page loads (heading + form + nav link)
- Preferences page loads (heading + nav link)

**Verify:** File is valid Robot syntax (parse check via `robot --dryrun`)

### Task 2: Update ROADMAP + STATE

**Action:**
- Mark Phase 11 complete in ROADMAP.md
- Update STATE.md: Phase 12, next = 12-01

### Task 3: Git commit

**Action:** `git add . && git commit -m "feat(phase-11): Notify full-stack..."`
