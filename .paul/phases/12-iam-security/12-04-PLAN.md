# Plan 12-04: Password Reset + Account Recovery + E2E Tests

**Phase:** 12 (IAM Security Completion)
**Depends on:** 12-01, 12-02, 12-03
**Autonomous:** true

## Objective

Password reset flow (forgot password → email token → set new password), account recovery (list backup methods), and Robot Framework E2E smoke tests for all Phase 12 auth flows + ROADMAP update marking Phase 12 complete.

## Acceptance Criteria

- [ ] DB: `fct_iam_password_reset_tokens` table (HMAC token, TTL, rate-limit)
- [ ] Backend: `POST /v1/auth/password-reset/request` + `POST /v1/auth/password-reset/complete`
- [ ] pytest: request (unknown → 200, known → 200), complete (invalid → 401, expired → 401, valid → 200)
- [ ] Frontend: "Forgot password?" link on signin page → `/auth/forgot-password` page + `/auth/password-reset` page
- [ ] E2E: Robot Framework smoke tests for magic-link, OTP, passkeys, and password reset signin tabs
- [ ] ROADMAP + STATE updated: Phase 12 marked complete, commit everything

## Tasks

### Task 1: DB migration 032

`"03_iam"."27_fct_iam_password_reset_tokens"` — (id, user_id FK, email, token_hash, expires_at, consumed_at, ip_address, created_at)
Rate-limit index on (email, created_at) for 3 per 15-min window.

### Task 2: Backend sub-feature 14_password_reset

`backend/02_features/03_iam/sub_features/14_password_reset/`
- `request`: rate-limit, HMAC token, notify fire-and-forget, always 200
- `complete`: hash lookup, expiry check, mark consumed, bcrypt new password, return session

### Task 3: Routes + wiring

`POST /v1/auth/password-reset/request` + `POST /v1/auth/password-reset/complete`
Wire into `iam/routes.py`

### Task 4: Tests

`tests/test_iam_password_reset.py`:
- Unknown email → 200 (no enumeration)
- Known email → 200 + token in DB
- Invalid token → 401
- Expired token → 401
- Valid token → 200 + password updated + session

### Task 5: Frontend

- `/auth/forgot-password` page — email input + "Send reset link" button
- `/auth/password-reset` page — token from query param + new password form
- "Forgot password?" link on signin password tab

### Task 6: E2E Robot + Final commit

- `tests/e2e/iam/12_auth_security.robot` — smoke tests: signin tabs visible, forgot-password link navigates, magic-link tab sends 200, otp tab sends 200, passkey tab visible, security page loads
- Update ROADMAP.md + STATE.md: Phase 12 complete
- git commit all Phase 12 work
