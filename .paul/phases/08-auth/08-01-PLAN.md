---
phase: 08-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/01_core/middleware.py
  - backend/02_features/03_iam/feature.manifest.yaml
  - backend/02_features/03_iam/routes.py
  - backend/main.py
  - backend/02_features/03_iam/sub_features/08_credentials/repository.py
  - backend/02_features/03_iam/sub_features/08_credentials/schemas.py
  - backend/02_features/03_iam/sub_features/08_credentials/service.py
  - backend/02_features/03_iam/sub_features/08_credentials/routes.py
  - backend/02_features/03_iam/sub_features/09_sessions/repository.py
  - backend/02_features/03_iam/sub_features/09_sessions/schemas.py
  - backend/02_features/03_iam/sub_features/09_sessions/service.py
  - backend/02_features/03_iam/sub_features/09_sessions/routes.py
  - backend/02_features/03_iam/sub_features/10_auth/schemas.py
  - backend/02_features/03_iam/sub_features/10_auth/service.py
  - backend/02_features/03_iam/sub_features/10_auth/routes.py
  - backend/02_features/03_iam/sub_features/10_auth/nodes/
  - tests/test_iam_auth_api.py
  - tests/test_iam_credentials_sessions_api.py
  - tests/test_iam_auth_oauth.py
autonomous: true
---

<objective>
## Goal
Wire the three new IAM auth sub-features (credentials/08, sessions/09, auth/10)
into the running app and get all pytest tests green.

## Purpose
The auth sub-features are fully coded but untracked. The modified wiring files
(middleware, manifest, routes, main.py) are staged/unstaged. This plan verifies
completeness, fixes any remaining gaps, and gets the test suite passing so
Phase 8 can move to the frontend.

## Output
- `tests/test_iam_auth_api.py` — all tests passing
- `tests/test_iam_credentials_sessions_api.py` — all tests passing
- `tests/test_iam_auth_oauth.py` — tests pass or documented skip (no OAuth
  credentials = skip is acceptable; the test file must not ERROR)
- Backend server starts cleanly with IAM module enabled
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## What already exists (no need to re-read, just be aware)
- `backend/02_features/03_iam/sub_features/08_credentials/` — 5-file structure ✓
- `backend/02_features/03_iam/sub_features/09_sessions/` — 5-file structure ✓
- `backend/02_features/03_iam/sub_features/10_auth/` — routes.py + schemas.py + service.py + nodes/ ✓
- `tests/test_iam_auth_api.py` (281 lines), `test_iam_credentials_sessions_api.py` (353 lines), `test_iam_auth_oauth.py` (236 lines) ✓
- Modified wiring: middleware.py (+63), feature.manifest.yaml (+108), iam/routes.py (+12) ✓
- DB tables for sessions/credentials already exist from Phase 3 schema (no new migrations needed)

## Key decisions from STATE.md
- Sessions use `"03_iam"."16_fct_sessions"` table (Phase 3)
- Credentials use `"03_iam"."22_dtl_credentials"` table (Phase 3)
- Session middleware is optional auth — injects user_id/session_id into request.state but does NOT reject
- Protected routes guard themselves by checking request.state.user_id
- Pool via NodeContext.extras['pool']; service fns take (pool, conn, ctx)
- UUID v7 for all new entities
- importlib everywhere for numeric dirs
- Audit via run_node("audit.events.emit", ...)
</context>

<acceptance_criteria>

## AC-1: Auth routes are mounted and reachable
```gherkin
Given the backend starts with iam module enabled
When GET /health is called
Then status 200 is returned
And GET /v1/catalog/nodes lists iam.auth.*, iam.credentials.*, iam.sessions.* nodes
```

## AC-2: Signup + signin + me + signout flow passes
```gherkin
Given a running DB with 03_iam schema applied
When test_iam_auth_api.py is executed
Then all test cases pass (0 failures, 0 errors)
```

## AC-3: Credentials + sessions API tests pass
```gherkin
Given a running DB with 03_iam schema applied
When test_iam_credentials_sessions_api.py is executed
Then all test cases pass (0 failures, 0 errors)
```

## AC-4: OAuth tests do not ERROR (skip is acceptable)
```gherkin
Given no OAuth credentials configured in env
When test_iam_auth_oauth.py is executed
Then tests are skipped (not errored) or pass if credentials are configured
And pytest exits with 0 failures (skips are not failures)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Audit wiring completeness and fix any gaps</name>
  <files>
    backend/01_core/middleware.py,
    backend/02_features/03_iam/feature.manifest.yaml,
    backend/02_features/03_iam/routes.py,
    backend/02_features/03_iam/sub_features/08_credentials/__init__.py,
    backend/02_features/03_iam/sub_features/09_sessions/__init__.py,
    backend/02_features/03_iam/sub_features/10_auth/__init__.py
  </files>
  <action>
    Read the following modified files in full and verify they are complete:

    1. `backend/01_core/middleware.py` — confirm session middleware:
       - Reads bearer token OR `x-session-token` header OR `tennetctl_session` cookie
       - Validates session via the sessions repo (or service) — must call sessions layer,
         NOT inline SQL
       - Injects user_id, session_id, org_id, workspace_id into request.state
       - Does NOT reject on missing/invalid session (just leaves state fields None)
       - `register_middleware(app)` wires both error handler + session middleware

    2. `backend/02_features/03_iam/feature.manifest.yaml` — confirm:
       - Sub-features 08_credentials, 09_sessions, 10_auth declared with correct keys
       - Node keys follow 3-segment format: `iam.credentials.*`, `iam.sessions.*`, `iam.auth.*`
       - Effect nodes have `emits_audit: true`

    3. `backend/02_features/03_iam/routes.py` — confirm:
       - Includes routers from credentials, sessions, and auth sub-features
       - Main `router` aggregates all sub-feature routers

    4. `backend/02_features/03_iam/sub_features/{08,09,10}/__init__.py` — must exist
       (even if empty) for Python imports to work

    Fix any gaps found. Common issues to look for:
    - Missing `__init__.py` files
    - Import paths using direct module imports instead of importlib
    - session_middleware trying to use pool before it's available (pool is on app.state)
    - Missing route prefix conflicts
  </action>
  <verify>
    Start the server (DB must be running first — `docker compose up -d` if needed):
    ```
    cd /Users/sri/Documents/tennetctl
    .venv/bin/python -m uvicorn backend.main:app --port 51734 --host 0.0.0.0 --reload
    ```
    Server must start without errors. Then:
    ```
    curl -s http://localhost:51734/health | python3 -m json.tool
    curl -s http://localhost:51734/v1/catalog/nodes | python3 -m json.tool | grep -E "iam\.(auth|credentials|sessions)"
    ```
    AC-1 satisfied when: server starts cleanly and catalog lists iam.auth.*, iam.credentials.*, iam.sessions.* nodes.
  </verify>
  <done>AC-1 satisfied: server starts, health returns 200, catalog nodes show auth sub-features</done>
</task>

<task type="auto">
  <name>Task 2: Run auth API tests to green</name>
  <files>
    tests/test_iam_auth_api.py,
    backend/02_features/03_iam/sub_features/10_auth/routes.py,
    backend/02_features/03_iam/sub_features/10_auth/service.py,
    backend/02_features/03_iam/sub_features/10_auth/schemas.py,
    backend/02_features/03_iam/sub_features/09_sessions/repository.py,
    backend/02_features/03_iam/sub_features/09_sessions/service.py
  </files>
  <action>
    Run the auth API tests:
    ```
    cd /Users/sri/Documents/tennetctl
    .venv/bin/python -m pytest tests/test_iam_auth_api.py -v --tb=short 2>&1
    ```

    Read the test file fully to understand what it tests (signup, signin, signout, /me,
    session expiry, validate_session node dispatch). Then fix failures in order:

    Common failure patterns in auth sub-features:
    - `pool.acquire()` called in service/repo instead of passing conn from route
    - `uuid4()` used instead of `_core_id.uuid7()`
    - Direct imports across sub-features instead of importlib
    - Missing `updated_at = CURRENT_TIMESTAMP` in UPDATE statements
    - Session token format mismatch (test expects specific token shape)
    - Cookie not being set on response (Response object not passed to route handler)
    - Audit emit missing on signup/signin/signout mutations

    Do NOT fix tests to match wrong behavior — fix the implementation.
    Do NOT add new endpoints beyond what the test calls.

    After all test_iam_auth_api.py tests pass, run credentials+sessions:
    ```
    .venv/bin/python -m pytest tests/test_iam_credentials_sessions_api.py -v --tb=short 2>&1
    ```

    Fix failures in credentials (08) and sessions (09) sub-features using the same
    patterns above. Read the test file to understand expected behavior before fixing.
  </action>
  <verify>
    ```
    .venv/bin/python -m pytest tests/test_iam_auth_api.py tests/test_iam_credentials_sessions_api.py -v 2>&1
    ```
    Must show 0 failures and 0 errors. Skips are acceptable.
  </verify>
  <done>AC-2 and AC-3 satisfied: both test files pass with 0 failures</done>
</task>

<task type="auto">
  <name>Task 3: Handle OAuth tests + full suite check</name>
  <files>
    tests/test_iam_auth_oauth.py,
    backend/02_features/03_iam/sub_features/10_auth/service.py
  </files>
  <action>
    Read `tests/test_iam_auth_oauth.py` fully. OAuth tests require external credentials
    (Google/GitHub client IDs and secrets). Acceptable outcomes:
    - Tests skip when env vars are missing (preferred — add `pytest.mark.skipif` if needed)
    - Tests pass if credentials are configured
    - Tests must NOT raise import errors or setup errors

    If the test file errors on import or conftest setup (before any test runs), fix the
    import/setup issue. The test file should gracefully skip when OAuth secrets are absent.

    Add skip markers like:
    ```python
    GOOGLE_CLIENT_ID = os.getenv("TENNETCTL_GOOGLE_CLIENT_ID", "")
    pytestmark = pytest.mark.skipif(
        not GOOGLE_CLIENT_ID,
        reason="OAuth credentials not configured — set TENNETCTL_GOOGLE_CLIENT_ID"
    )
    ```

    Run all three test files together for final check:
    ```
    .venv/bin/python -m pytest tests/test_iam_auth_api.py tests/test_iam_auth_oauth.py tests/test_iam_credentials_sessions_api.py -v 2>&1
    ```
    Also run the full non-migrator suite to confirm no regressions:
    ```
    .venv/bin/python -m pytest tests/ --ignore=tests/test_migrator.py -v --tb=short 2>&1
    ```
  </action>
  <verify>
    Full suite (ex-migrator):
    ```
    .venv/bin/python -m pytest tests/ --ignore=tests/test_migrator.py -q 2>&1 | tail -5
    ```
    Must show 0 failures. Skips are fine.
  </verify>
  <done>AC-4 satisfied: oauth tests skip or pass; full suite ex-migrator shows 0 failures</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `backend/01_catalog/` — NCP v1 runner and catalog (locked)
- `backend/02_features/02_vault/` — vault is complete and committed
- `tests/test_migrator.py` — pre-existing drift, excluded from suite
- Any existing IAM sub-features 01–07 (orgs, workspaces, users, roles, groups, applications, memberships)
- Database schema — no new SQL migrations in this plan

## SCOPE LIMITS
- This plan is backend only — no frontend changes
- Do not add new API endpoints beyond what the tests already call
- Do not wire OAuth providers until test_iam_auth_oauth.py is read and understood
- Do not touch vault bootstrap or crypto

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `docker compose up -d` (or DB confirmed running on port 5434)
- [ ] Server starts: `.venv/bin/python -m uvicorn backend.main:app --port 51734 --host 0.0.0.0`
- [ ] Health check: `curl http://localhost:51734/health` → `{"ok":true}`
- [ ] Auth nodes in catalog: `curl http://localhost:51734/v1/catalog/nodes` lists iam.auth.*, iam.sessions.*, iam.credentials.*
- [ ] `.venv/bin/python -m pytest tests/test_iam_auth_api.py -v` → 0 failures
- [ ] `.venv/bin/python -m pytest tests/test_iam_credentials_sessions_api.py -v` → 0 failures
- [ ] `.venv/bin/python -m pytest tests/test_iam_auth_oauth.py -v` → 0 failures (or all skipped)
- [ ] `.venv/bin/python -m pytest tests/ --ignore=tests/test_migrator.py -q` → 0 failures
</verification>

<success_criteria>
- All tasks completed
- Backend server starts without errors with iam module enabled
- Auth routes respond correctly (verified via pytest)
- No regressions in existing tests (vault, IAM sub-features 01-07)
</success_criteria>

<output>
After completion, create `.paul/phases/08-auth/08-01-SUMMARY.md`
</output>
