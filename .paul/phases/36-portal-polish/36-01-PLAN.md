---
phase: 36-portal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/config/features.ts
autonomous: true
---

<objective>
## Goal
Register the new `/system/health` page in the primary nav so admins can discover it, and add a "System" feature to the sidebar config.

## Purpose
Phase 35-03 shipped `/system/health`, but it's orphaned — no nav link points to it. Same for future system surfaces (Module toggle inspector, worker status, migration history). Adding a "System" feature with one sub-feature now creates the shape for future additions.

## Output
- `System` entry added to `FEATURES` in `frontend/src/config/features.ts`
</objective>

<context>
@frontend/src/config/features.ts
@frontend/src/app/(dashboard)/system/health/page.tsx
</context>

<acceptance_criteria>

## AC-1: System feature visible in sidebar
```gherkin
Given the admin navigates to /system/health
When the sidebar renders
Then the active feature is "System"
And the sub-feature "Health" is the selected nav item

Given the FEATURES constant is inspected
Then an entry with key "system", basePath "/system" exists
And its subFeatures include { href: "/system/health", label: "Health", testId: "nav-system-health" }
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add System feature to FEATURES</name>
  <files>frontend/src/config/features.ts</files>
  <action>
    Insert a new `FeatureNav` entry between "nodes" and end-of-array (keep order: System sits after Node Catalog since both are platform-internal):
    ```
    {
      key: "system",
      label: "System",
      description: "Platform operations: module status, pool depth, worker lag, migration history.",
      basePath: "/system",
      testId: "nav-feature-system",
      subFeatures: [
        { href: "/system/health", label: "Health", testId: "nav-system-health" },
      ],
    }
    ```
    Avoid: touching the existing FEATURES entries or the active* helper functions.
  </action>
  <verify>cd frontend && npx tsc --noEmit && npx next build</verify>
  <done>AC-1 satisfied: System feature appears and /system/health is reachable from sidebar</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any other feature's nav entries
- The activeFeature / activeSubFeatureHref helpers
- Any existing pages

## SCOPE LIMITS
- No migration of apiFetch → @tennetctl/sdk (deferred — see below)
- No Portal Views wiring (deferred to v0.1.8 or later)
- No new loading/empty/error state refactors (current `ui.tsx` primitives are already standardized)
- This plan intentionally compressed vs. MILESTONE-QUEUE v0.2.4 scope

</boundaries>

<verification>
- [ ] `npx tsc --noEmit` clean
- [ ] `npx next build` succeeds
- [ ] FEATURES contains "system" entry
</verification>

<success_criteria>
- Sidebar shows "System" with Health link when on /system/*
- Build green
</success_criteria>

<output>
After completion, create `.paul/phases/36-portal-polish/36-01-SUMMARY.md`
</output>
